import building.BuildingController;

/**
 * Main class for launching the Building Elevator System Simulation.
 * It initializes the application by creating an instance of BuildingController.
 */
public class Main {
  /**
   * Main method to launch the application. It initializes the BuildingController,
   * which sets up and starts the user interface.
   *
   * @param args not used.
   */
  public static void main(String[] args) {
    BuildingController controller = new BuildingController();
  }
}

